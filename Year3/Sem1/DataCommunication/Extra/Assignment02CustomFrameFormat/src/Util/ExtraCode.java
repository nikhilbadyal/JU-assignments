package Util;

public class ExtraCode {
    /* if(maxMessageLength < 0 ){
            throw  new ImplementationError("MTU not large enough to send the frame.");
        }else if(maxMessageLength > maxMessage ){
            maxMessageLength = maxMessage;
        }*/
}
