#include<iostream>
using namespace std;
int main()
{

    cout<<"Ente"
}
void LinkedList::search_no(int no)
{
	struct Node *p,*q;
	p=head;
	q=head->next;
	if(p->data == no)
	{
		cout<<"Element found."
	}
	else if {

    while(q->data != no)
    {
    	p=p->next;
    	q=q->next;
	}
	p->next=q->next;
	else{
        cout<<"Element not found.";
	}
	}
}
